# HonorInk App Improvements Plan

## Priority Order

### 1. Critical Issues (Fix Immediately)

| Issue | Location | Fix |
|-------|----------|-----|
| Duplicate ThemeProvider | `App.jsx:29` and `main.jsx:27` | Remove ThemeProvider wrapper from App.jsx - keep only in main.jsx |
| Missing Error Handling | `UdemyCertificate.jsx:93,140` | Add toast/notification for export failures instead of just console.error |
| Missing Error Handling | `LinkedInCertificate.jsx:113` | Add toast/notification for export failures |
| localStorage without quota check | `WelcomeModal.jsx`, `CookieConsent.jsx`, `useFormDraft.js` | Add try-catch with size validation |

### 2. High Priority (Accessibility & Performance)

| Issue | Location | Fix |
|-------|----------|-----|
| Missing ARIA label | `LinkedInCertificate.jsx:134` | Add `aria-label="Go back"` to back link |
| Missing ARIA label | `CourseraCertificate.jsx` | Check and add aria-labels |
| Code Duplication - PDF options | `UdemyCertificate.jsx:105-128`, `LinkedInCertificate.jsx:79-101` | Extract to shared utility in `lib/utils.js` |
| Duplicated formatDate | `UdemyCertificate.jsx:145-153`, `LinkedInCertificate.jsx:118-126` | Move to `lib/utils.js` |
| localStorage on mount without SSR check | Multiple components | Add useEffect check or use lazy-init pattern |

### 3. Medium Priority (Code Quality & UX)

| Issue | Location | Fix |
|-------|----------|-----|
| Email regex too permissive | `Contact.jsx` | Use stricter `/^[^\s@]+@[^\s@]+\.[a-zA-Z]{2,}$/` |
| Remove console.error statements | Various files | Clean up production code |
| Remove unused imports | Various files | Run ESLint to find dead code |
| PropTypes missing | `CertificateForm.jsx` | Add PropTypes for props validation |

### 4. Additional Recommendations

- Add input sanitization for user-generated content
- Consider Content Security Policy headers
- Memoize components with React.memo where appropriate
- Optimize html2canvas scale settings (currently 3-4, consider 2 for performance)
- Lazy-load certificate components (already using lazy, verify consistent)
- Add error boundary tests
- Improve SEO component to avoid DOM recreation

---

## Implementation Notes

1. **ThemeProvider**: The ThemeProvider is wrapped twice - in `main.jsx:27` and `App.jsx:29`. This can cause state conflicts. Remove from App.jsx.

2. **PDF Export Utility**: Create a shared function `exportToPDF(element, options)` in utils that all certificate components can use.

3. **Date Formatting**: Create `formatCertificateDate(dateString)` in utils.js.

4. **Error Handling**: Add a toast component or use existing UI to show export errors to users.