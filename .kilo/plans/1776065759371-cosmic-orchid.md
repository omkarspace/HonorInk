# Plan: Feedback Loop & Communications

## Overview
Add contact email, bug report option, automated welcome modal, and enhanced empty states to guide new users.

## Tasks

### 1. Contact/Support Email
- Add `support@honorink.app` to the footer in `Home.jsx`
- Create new `/contact` page with contact form and email display

### 2. Bug Report Option
- Add "Report a Bug" link in footer (links to contact page with bug category)
- Include bug category option in contact form

### 3. Automated "Welcome" Modal
- Create `WelcomeModal` component that shows on first visit
- Use localStorage to track if user has seen the welcome modal (`honorink_welcome_shown`)
- Show quick-start guide with steps:
  1. Select certificate platform
  2. Fill in your details
  3. Download your certificate
- Add "Don't show again" option

### 4. Basic User Onboarding (Empty States)
- Enhance form placeholders with helpful guidance in `CertificateForm.jsx`:
  - Name field: "e.g., John Doe"
  - Course field: "e.g., Introduction to React"
  - Date field: "e.g., January 2024"
- Add tooltip hints on focus for each field

## Implementation Order
1. Create `WelcomeModal` component with localStorage check
2. Add WelcomeModal to App.jsx (renders on first visit)
3. Add contact email + bug report to Footer in Home.jsx
4. Create `/contact` route and Contact page (optional - can start with footer email)
5. Enhance form placeholders in CertificateForm.jsx

## Files to Modify
- `src/components/WelcomeModal.jsx` (new)
- `src/App.jsx` (add WelcomeModal)
- `src/components/Home.jsx` (footer updates)
- `src/components/CertificateForm.jsx` (placeholder enhancements)

## Files to Create
- `src/pages/Contact.jsx` (optional - contact page)
